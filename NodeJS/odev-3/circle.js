const circleArea = (r) => {
    const area = 3 * (r*r)
    console.log(area);
}

const circleCircumference = (r) => {
    const circ = 2 * 3 * r
    console.log(circ);
}

module.exports = { circleArea, circleCircumference };
